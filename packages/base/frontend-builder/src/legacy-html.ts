/** Lazy entry for the legacy HTML/GrapesJS builder (loads grapesjs + monaco on import). */
export { FrontendBuilderVanilla } from './FrontendBuilder.vanilla';
export type { FrontendBuilderProps } from './types';
