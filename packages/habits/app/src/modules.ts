/**
 * Default modules list for `habits init` command.
 * Re-exports from @ha-bits/core for consistency.
 */
export { defaultModules, type ModuleDefinition, type ModulesConfig } from '@ha-bits/core';
