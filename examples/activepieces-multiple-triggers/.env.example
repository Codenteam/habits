# IMAP Configuration for GreenMail (local testing)
# GreenMail auto-creates any user account on first access
# Ports: SMTP=3025, IMAP=3143, IMAPS=3993

IMAP_HOST=localhost
IMAP_USERNAME=test@localhost
IMAP_PASSWORD=testpassword
IMAP_PORT=3143
