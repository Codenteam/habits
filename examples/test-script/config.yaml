version: "1.0"

workflows:
  - id: test-script-workflow
    path: ./workflow.yaml
    enabled: true

server:
  port: 13000
  host: 0.0.0.0

options:
  timeout: 60000
  retryOnFailure: false
  logLevel: info
