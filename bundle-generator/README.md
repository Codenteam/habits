# Bundle Generator

## Commands

```bash
# Test build
npm run test:build
# or
node test-build.js

# Test run
npm run test:run
# or
node test-run.js
```

## For local development
Make sure to link all bits and link @ha-bits/cortex-core, you can use dev.ts for that, for example: 