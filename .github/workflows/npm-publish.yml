name: NPM Publish

on:
  push:
    branches:
      - main
      - 'release/**'
    tags:
      - 'release/**'

permissions:
  contents: read
  id-token: write

jobs:
  publish-to-npm:
    runs-on: ubuntu-latest
    environment: npm
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          cache: true
          version: 10

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Publish packages
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
          GITHUB_RUN_NUMBER: ${{ github.run_number }}
        run: npx tsx scripts/npm-publish.ts | npx tsx scripts/log-sanitizer.ts

      - name: Deploy to server
        env:
          STAGING_HUB_SSH_KEY: ${{ secrets.STAGING_HUB_SSH_KEY }}
          STAGING_HUB_SSH_HOST: ${{ secrets.STAGING_HUB_SSH_HOST }}
          STAGING_HUB_SSH_USER: ${{ secrets.STAGING_HUB_SSH_USER }}
          STAGING_HUB_SSH_PORT: ${{ secrets.STAGING_HUB_SSH_PORT }}
          PROD_HUB_SSH_KEY: ${{ secrets.PROD_HUB_SSH_KEY }}
          PROD_HUB_SSH_HOST: ${{ secrets.PROD_HUB_SSH_HOST }}
          PROD_HUB_SSH_USER: ${{ secrets.PROD_HUB_SSH_USER }}
          PROD_HUB_SSH_PORT: ${{ secrets.PROD_HUB_SSH_PORT }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
        run: npx tsx scripts/deploy.ts | npx tsx scripts/log-sanitizer.ts

