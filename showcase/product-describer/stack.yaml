version: "1.0"

workflows:
  - id: describe-product
    path: ./habits/describe-product.yaml
    enabled: true

  - id: list-products
    path: ./habits/list-products.yaml
    enabled: true

  - id: get-product
    path: ./habits/get-product.yaml
    enabled: true

server:
  port: 13000
  host: "0.0.0.0"
  frontend: ./frontend
  openapi: true

logging:
  level: info
  outputs: [console]
  format: text
  colorize: true
