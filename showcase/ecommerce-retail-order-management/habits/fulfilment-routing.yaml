id: fulfilment-routing
name: Fulfilment Routing
description: Placeholder workflow for showcase dashboard (tailored deployment required)

input: []

nodes:
  - id: ready
    type: script
    data:
      framework: script
      source: inline
      params:
        type: script
        language: javascript
        script: |
          async function main() {
            return { status: 'ready' };
          }

output:
  status: "{{ready.result.status}}"
