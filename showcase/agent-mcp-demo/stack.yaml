version: "1.0"
name: "Agent MCP Demo"
workflows:
  - id: agent-mcp-search
    path: ./habit.yaml
    enabled: true

server:
  port: 13000
  host: "0.0.0.0"

logging:
  level: info
  outputs: [console]
  format: text
  colorize: true
