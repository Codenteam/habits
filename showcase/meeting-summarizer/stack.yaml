version: "1.0"

workflows:
  - id: summarize
    path: ./habits/summarize.yaml
    enabled: true

  - id: list-meetings
    path: ./habits/list-meetings.yaml
    enabled: true

  - id: get-meeting
    path: ./habits/get-meeting.yaml
    enabled: true

server:
  port: 13000
  host: "0.0.0.0"
  frontend: ./frontend
  openapi: true

logging:
  level: info
  outputs: [console]
  format: text
  colorize: true
