version: "1.0"
name: "Test Local AI New"

bits:
  - id: localAiNew
    module: "@ha-bits/bit-local-ai"

workflows:
  - id: test-install-model
    path: ./habits/test-install-model.yaml
    enabled: true

  - id: test-generate-text
    path: ./habits/test-generate-text.yaml
    enabled: true

  - id: test-embed-text
    path: ./habits/test-embed-text.yaml
    enabled: true

server:
  port: 13000
  host: "0.0.0.0"
  openapi: true

logging:
  level: info
  outputs: [console]
  format: text
  colorize: true
