version: "1.0"
name: "Script Test"
description: Tests JavaScript script node execution in Tauri

workflows:
  - id: greet
    path: ./greet.yaml
    enabled: true

server:
  port: 13001
  host: "0.0.0.0"

logging:
  level: info
  outputs: [console]
  format: text
  colorize: true
