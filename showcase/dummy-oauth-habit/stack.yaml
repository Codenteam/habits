version: "1.0"
name: "Dummy OAuth Habit"
description: Minimal stack for Tauri OAuth connect testing

workflows:
  - id: dummy-oauth-habit
    path: ./habit.yaml
    enabled: true

server:
  port: 13000
  host: "0.0.0.0"
  frontend: ./frontend

logging:
  level: info
  outputs: [console]
  format: text
  colorize: true
