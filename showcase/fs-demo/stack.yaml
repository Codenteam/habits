version: "1.0"
workflows:
  - id: fs-demo
    path: ./habit.yaml
    enabled: true
server:
  port: 13001
  host: "0.0.0.0"
  frontend: ./frontend

logging:
  level: info
  outputs: [console]
  format: text
  colorize: true
