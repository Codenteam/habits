version: "1.0"
workflows:
  - id: capture-lead
    path: ./habits/capture-lead.yaml
    enabled: true
  - id: enrich-lead
    path: ./habits/enrich-lead.yaml
    enabled: true
  - id: sync-crm
    path: ./habits/sync-crm.yaml
    enabled: true
server:
  port: 13000
  host: "0.0.0.0"
  frontend: ./frontend
  openapi: true
logging:
  level: info
  outputs: [console]
  format: text
  colorize: true
