version: "1.0"
name: "Voice Demo"
workflows:
  - id: voice-capture
    path: ./voice-capture.habit.yaml
    enabled: true
  - id: voice-history
    path: ./voice-history.habit.yaml
    enabled: true

server:
  port: 13001
  host: "0.0.0.0"

logging:
  level: info
  outputs: [console]
  format: text
  colorize: true
