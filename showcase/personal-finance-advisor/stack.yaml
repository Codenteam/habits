version: "1.0"

workflows:
  - id: analyze-finances
    path: ./habits/analyze-finances.yaml
    enabled: true

  - id: create-budget
    path: ./habits/create-budget.yaml
    enabled: true

  - id: get-advice
    path: ./habits/get-advice.yaml
    enabled: true

  - id: list-reports
    path: ./habits/list-reports.yaml
    enabled: true

server:
  port: 13000
  host: "0.0.0.0"
  frontend: ./frontend
  openapi: true

logging:
  level: info
  outputs: [console]
  format: text
  colorize: true
