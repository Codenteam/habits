version: '1.0'
workflows:
  - id: study-flashcards
    path: ./study-flashcards.yaml
    enabled: true
    webhookTimeout: 30000
server:
  port: 15000
  host: 0.0.0.0
  frontend: ./frontend
defaults:
  webhookTimeout: 30000
