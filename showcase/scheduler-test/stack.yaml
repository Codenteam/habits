version: "1.0"
name: "Scheduler Test"
workflows:
  - id: scheduler-test
    path: ./habit.yaml
    enabled: true
server:
  port: 13000
  host: "0.0.0.0"

logging:
  level: debug
  outputs: [console]
  format: text
  colorize: true
