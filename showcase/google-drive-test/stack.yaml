version: "1.0"
name: "Google Drive Test"
workflows:
  - id: google-drive-test
    path: ./habits/google-drive-test.yaml
    enabled: true

server:
  port: 13000
  host: "0.0.0.0"
  frontend: ./frontend
  openapi: true

logging:
  level: info
  outputs: [console]
  format: text
  colorize: true
