id: get-quiz
name: Get Quiz
description: Get a specific quiz by ID

input:
  - id: id
    type: string
    required: true

nodes:
  - id: fetch-quiz
    type: bits
    data:
      framework: bits
      source: npm
      module: "@ha-bits/bit-database"
      operation: get
      params:
        collection: "quizzes"
        key: "{{habits.input.id}}"

edges: []

output:
  quiz: "{{fetch-quiz.value}}"
  found: "{{fetch-quiz.found}}"
